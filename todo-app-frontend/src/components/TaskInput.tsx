const TaskInput = () => {
    return (
        <div className="task-input">
            <input type="text" placeholder="New task..." />
            <button>Add</button>
        </div>
    );
};

export default TaskInput;